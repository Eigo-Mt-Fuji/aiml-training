https://qiita.com/soarflat/items/d5015bec37f8a8254380
環境変数について以下の記事のように `~/.bashrc` や `~/.bash_profile` に記述することで、都度exportコマンドで設定する手間を省くことができますので 試してみて下さいmm
環境変数はenvコマンドでセットされているのを確認。ただ、RubyMindを再起動する度に環境変数からcxとapikeyが消えるのが気になりました
この場合、どのように対応するべきでしょうか？

既に環境変数を設定したタブでrailsのサーバを起動したら検索画面を表示することが出来ました！
後から設定した環境変数って保存自体は一時的にしか出来ないものなのでしょうか？
